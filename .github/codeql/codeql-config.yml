name: "ECS Agent Security Scan"

# Focus alerts on Windows-related code where command injection risk is highest
paths:
  - '**/*windows*.go'

paths-ignore:
  - '**/*_test.go'
  - '**/mock_*.go'
  - '**/mocks/**'
  - vendor/**

queries:
  - uses: security-extended
  - uses: security-and-quality
